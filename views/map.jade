//- place map stuff here

div.infobox 
  h4 Just nu visas alla {{selectedCaregiver.name}} vårdboenden! 

google-map(center="map.center", zoom="map.zoom", options="map.options", control="mapControl", draggable="true")
  marker(ng-repeat="house in mapPoints | filter: house.caregiver = selectedCaregiver", idKey="house.id", coords="house.coordinates", click="showHouse(house)")

    window(show="house.show", ng-class="{active: caregiverIsSelected(house)}")
      div
        p
          strong {{house.name}}
          |, {{house.caregiver}}
        p
          |{{house.address}}
